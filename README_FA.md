# DjangoMessenger  
یک موتور پیام‌رسان چندکاناله و قابل توسعه برای جنگو  

DjangoMessenger یک فریم‌ورک پیام‌رسانی قدرتمند و انعطاف‌پذیر برای جنگو است که با معماری تمیز (Clean Architecture) و معماری شش‌ضلعی (Hexagonal) طراحی شده است.  
این سیستم پیام‌سازی (Builder) را از پیام‌رسانی (Sender) جدا می‌کند و این امکان را فراهم می‌کند که پیام‌ها به شکل استاندارد ساخته شده و از طریق کانال‌های مختلفی مانند:

- SMS  
- Email  
- Telegram  
- Push  
- یا هر کانال سفارشی که خودتان پیاده‌سازی می‌کنید  

ارسال شوند.

---

## 🎯 ویژگی‌های اصلی

### ✔ جداسازی کامل ساخت پیام از ارسال پیام
با استفاده از **Builder Interface**، محتوا و ساختار پیام تولید می‌شود.  
و با استفاده از **Sender Interface**، پیام از طریق کانال مورد نظر ارسال می‌شود.

### ✔ پشتیبانی از Metadata Itemها
سیستم از ساخت عناصر UI مانند:

- Button  
- Form  
- Input  
- Checkbox  
- Radio  
- Submit  

پشتیبانی می‌کند.  
هر Sender مسئول رندر کردن این عناصر بر اساس فرمت مخصوص خود است.

### ✔ معماری Plug-in‌محور
تمام اجزاء زیر قابل اضافه شدن و توسعه هستند:

- Builderهای جدید  
- Senderهای جدید  
- Metadata Itemهای جدید  
- Permissionهای جدید  

### ✔ Permission Layer
هر پیام قبل از ساخت شدن یا ارسال شدن می‌تواند با مجموعه‌ای از Permissionها بررسی شود:

- Verified  
- NotVerified  
- AllowAny  
- Custom User Permission  

و کاربر می‌تواند Permissionهای جدید بسازد.

### ✔ پایگاه‌داده داخلی خصوصی
ماژول دارای یک مدل داخلی (private) برای ذخیره‌سازی:

- تنظیمات کانال  
- وضعیت فعال/غیرفعال بودن  
- وضعیت تأیید (Verification)  
- سایر داده‌های لازم  

است و مستقیماً در دسترس کاربر نهایی نیست.

---

## 🚀 معماری سیستم

پروژه بر اساس چند Component اصلی طراحی شده:

### **1. Main Component**
هسته سیستم شامل Facade و Factory برای مدیریت جریان ساخت/ارسال پیام.

### **2. Business Logic Component**
شامل Builder Interface و پیاده‌سازی‌های آن.

### **3. Sender Setup Component**
شامل Sender Interface و کانال‌های مختلف ارسال پیام.

### **4. Metadata Setup Component**
ساختارهای فرم، دکمه، ورودی و ... همراه با رندرهای مخصوص هر Sender.

### **5. Permission Component**
برای کنترل دسترسی، مانند اجازه ارسال پیام یا اجازه ساخت پیام.

---

## 📦 نصب

(هنوز منتشر نشده)  
پس از انتشار روی PyPI:

```bash
pip install djangomessenger
```

و اضافه کردن به تنظیمات:

```python
INSTALLED_APPS = [
    ...
    "message_engine",
]
```

---

## 🧩 استفاده ساده (دمو)

```python
from djangomessenger import facade

facade.send(
    user=user,
    message_type="welcome",
    channel="sms",
    data={"username": "ali"}
)
```

---

## 📚 توسعه‌دهندگان

### افزودن یک Sender جدید:

```python
class MyCustomSender(SenderInterface):
    def render_button(self, item):
        ...

    def send(self, payload):
        ...
```

و سپس Registry:

```python
register_sender("mychannel", MyCustomSender)
```

---

## 📄 لایسنس
MIT License

---

## 👤 مشارکت
Pull Request و Issue کاملاً پذیرفته می‌شود.

